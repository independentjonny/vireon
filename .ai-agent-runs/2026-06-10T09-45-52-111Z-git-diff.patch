 tools/neven-agent/agent-server.ts | 15 ++++++++++++++-
 1 file changed, 14 insertions(+), 1 deletion(-)
diff --git a/tools/neven-agent/agent-server.ts b/tools/neven-agent/agent-server.ts
index 70c497c..4d1413a 100644
--- a/tools/neven-agent/agent-server.ts
+++ b/tools/neven-agent/agent-server.ts
@@ -24,7 +24,8 @@ type AgentAction =
   | "autoImproveUI"
   | "planFix"
   | "executeFix"
-  | "autofix";
+  | "autofix"
+  | "autoLoop";
 
 type AgentRequest = {
   action?: AgentAction;
@@ -296,6 +297,18 @@ async function runStructuredAction(request: AgentRequest) {
     case "evaluateFix":
       return evaluateFix();
 
+    case "autoLoop": {
+      const stages = ["autoImproveUI", "planFix", "executeFix", "evaluateFix"] as const;
+      const report: string[] = [];
+
+      for (const action of stages) {
+        const output = await runStructuredAction({ ...request, action });
+        report.push([`## ${action}`, output].join("\n"));
+      }
+
+      return summarizeOutput(report.join("\n\n"), 30_000);
+    }
+
     case "reviewUI": {
       return reviewUI();
     }
