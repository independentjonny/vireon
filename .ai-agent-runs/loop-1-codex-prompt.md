Fix only the mobile hamburger menu in `src/app/components/MobileNav.tsx`.

Requirements:
- Work only inside the current repo.
- Modify **only** `src/app/components/MobileNav.tsx`.
- Make the smallest possible targeted change.
- Fix the root cause of the mobile hamburger menu issue; do not add unrelated features or cosmetic rewrites.
- Do not modify formatting broadly or refactor unrelated code.
- Do not touch any other files.

Validation:
- Run `npm run build`.
- If the fix can be verified with an existing UI/Playwright check without adding files, run it as well.
- Stop after one successful fix and successful build.

Before finishing:
- Confirm only `src/app/components/MobileNav.tsx` changed.
- Summarise the exact change made.
- Summarise test/build results.