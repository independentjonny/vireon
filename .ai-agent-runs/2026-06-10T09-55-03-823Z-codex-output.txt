Modified only `tools/neven-agent/agent-server.ts`.

Updated `generateUIReviewPlaceholder()` to detect when `page.tsx` already has `w-full` plus `lg:px-4` or smaller. In that case, it no longer recommends widening the layout and instead points the recommendation at `src/app/components/OverviewV3.tsx` for hero dashboard density improvements. Tests were not run per instruction.
