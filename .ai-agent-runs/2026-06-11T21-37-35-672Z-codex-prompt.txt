
You are working in the Neven repo at C:\Users\summe\liberva.

Goal:
Modify only tools/neven-agent/agent-server.ts. Improve evaluateFix for OverviewV3 hero tasks. PASS only if git diff for src/app/components/OverviewV3.tsx includes structural JSX changes to the hero KPI grid or Financial Health render block. A change only to the secondaryMetrics array or accentClass must return FAIL. Check the diff text for className grid changes, removal or movement of the standalone Financial Health block, or JSX rendering changes involving Financial Health. Return summary only.

Rules:
- Follow the goal exactly.
- Make the smallest targeted change.
- Do not rewrite unrelated files.
- Do not modify package.json, package-lock.json, playwright.config.ts, next.config.ts, .ai/*, or API routes unless the goal explicitly requires it.
- Do not run npm run build unless the goal explicitly asks for it.
- Do not run tests unless the goal explicitly asks for them.
- If the goal asks to read or inspect only, do not modify files.
- At the end, summarize exactly what you did.
