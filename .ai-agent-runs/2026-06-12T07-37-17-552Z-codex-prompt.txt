
You are working in the Neven repo at C:\Users\summe\liberva.

Goal:
Modify only tools/neven-agent/agent-server.ts. Big build: upgrade aiReviewUI and aiAutoLoop. aiReviewUI must return strict JSON with fields: issue, likelyFile, targetArea, recommendedTask, confidence, currentScore, successCriteria. currentScore must be a number from 1 to 10 based only on the screenshots. targetArea examples: hero-kpi-row, mobile-navigation, dashboard-spacing, portfolio-health-section, import-workflow. aiAutoLoop must parse this JSON, run Codex with a task that includes likelyFile, targetArea, recommendedTask, and successCriteria, then capture after screenshots, call aiReviewUI again for an afterScore, and return beforeScore, afterScore, improved true/false, selected task, Codex output, typecheck, git diff, and screenshot paths. If afterScore is lower than beforeScore, automatically restore modified tracked app files but do not restore tools/neven-agent/agent-server.ts. Do not auto-commit. Return summary only.

Rules:
- Follow the goal exactly.
- Make the smallest targeted change.
- Do not rewrite unrelated files.
- Do not modify package.json, package-lock.json, playwright.config.ts, next.config.ts, .ai/*, or API routes unless the goal explicitly requires it.
- Do not run npm run build unless the goal explicitly asks for it.
- Do not run tests unless the goal explicitly asks for them.
- If the goal asks to read or inspect only, do not modify files.
- At the end, summarize exactly what you did.
